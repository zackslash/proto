syntax = "proto3";

package sockets;

message SocketMessage {
    string channel = 1;
    string action = 2;
    string payload = 3;
}

message PublishResponse {
    bool success = 1;
    string error = 2;
}

service Sockets {
    rpc Publish(SocketMessage) returns (PublishResponse);
}